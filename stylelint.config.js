const sharedConfig = require("@teamshares/design-system/configs/stylelint.config.js");

// Hook for any application-specific config overrides
const config = Object.assign({}, sharedConfig, {});

module.exports = config;
